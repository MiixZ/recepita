const {
  DB_HOST,
  DB_USER,
  DB_PASSWORD,
  DB_NAME,
  DB_PORT,
  PORT,
  NODE_ENV,
  LOG_SERVER_URL,
} = process.env;

export {
  DB_HOST,
  DB_USER,
  DB_PASSWORD,
  DB_NAME,
  DB_PORT,
  PORT,
  NODE_ENV,
  LOG_SERVER_URL,
};
